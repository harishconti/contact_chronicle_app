name: contact_chronicle
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  google_fonts: ^6.1.0
  intl: ^0.19.0
  provider: ^6.0.5
  lucide_flutter: ^0.542.0 # Updated version
  image_picker: ^1.0.4 # Check for the latest version
  url_launcher: ^6.1.14 # Check for the latest version
  shared_preferences: ^2.2.2 # Enabled for persisting settings

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg
